# Python
__pycache__/
*.py[cod]
.venv/
.uv/
dist/
*.egg-info/

# Config (contains personal paths/preferences)
imagecli.toml

# Generated images — default sink is now ~/.roxabi/imagecli/out/ (outside repo).
# This entry stays as a safety net for users whose imagecli.toml still points
# the output_dir at the legacy in-repo path.
images/images_out/

# Models cache (huge, managed by HuggingFace hub)
# ~/.cache/huggingface/ — outside repo, not needed here

# Claude config (personal, not shared)
.claude/settings.local.json
.claude/worktrees/

# Dev
.ruff_cache/
.pytest_cache/

.env
